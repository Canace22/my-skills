------------------------------------------------
name: ai-project-summary
description: 在项目、功能开发或 bug 修复完成后，让 AI 生成一份结构化的项目总结并提交到代码仓库。\
当用户说"写个项目总结"、"记录一下这次改动"、"生成 changelog"、"项目收尾"、\
"写个 handoff 文档"、"记录下来"时触发。也适用于多轮对话结束时主动建议用户保存总结。
------------------------------------------------

# AI Project Summary Skill

在项目结束时，让 AI 生成一份高质量的项目总结，commit 进仓库，供未来的人（或 AI）查阅。

## 工作流

### 1. 收集上下文

从当前对话 / 用户提供的信息中提取：

* 解决了什么问题？（背景 + 动机）

* 做了哪些改动？（文件、模块、逻辑）

* 关键设计决策是什么？为什么这样选？

* 遗留问题 / 后续待办？

如果信息不足，向用户追问 1-2 个最关键的问题，不要连续问一堆。

### 2. 生成总结文档

输出一份结构化 Markdown，包含以下部分（按需取舍）：

```markdown
# [项目/功能名称] — 改动说明

## 背景
（为什么要做这件事）

## 改动概述
（做了什么，高层次描述，不是逐行注释）

## 关键决策
（重要的技术选择 + 简要理由）

## 影响范围
（涉及的模块 / 接口 / 依赖）

## 已知问题 / 后续
（遗留 TODO，或下一步建议）
```

**写作原则：**

* 高层次概述，不是流水账

* 面向"三个月后的陌生人"（可能是你自己）

* 避免废话，每个段落都要有实际信息量

* 不要加"Approved-by"或类似的自我引用段落

### 3. 用户审阅

生成后，明确提示用户：

> 请仔细阅读并确认内容准确，修改后再提交。这份文档代表你的判断，不只是 AI 的输出。

### 4. 提交建议

提供可直接使用的命令：

```bash
# 建议存放路径
docs/summaries/YYYY-MM-DD-<feature-name>.md

# 提交
git add docs/summaries/
git commit -m "docs: add project summary for <feature-name>"
```

***

## 文件命名约定

```text
docs/summaries/
├── 2024-03-15-auth-refactor.md
├── 2024-04-02-payment-bugfix.md
└── 2024-05-10-search-feature.md
```

便于 `git grep` 检索。

***

## 可选：维护 CLAUDE.md 中的约定

如果项目使用 `CLAUDE.md`，可以加一条：

```markdown
## 项目总结约定
每次较大改动结束后，在 docs/summaries/ 生成并提交一份总结文档。
格式参考现有文件。不要在总结中添加"Approved-by"类段落。
```

***

## 触发示例

* "这个功能做完了，帮我记录一下"

* "写个 handoff 文档"

* "生成项目总结，我要 commit 进去"

* "这次重构改了什么，帮我整理"

* 对话末尾用户说"差不多了"——可主动建议生成总结